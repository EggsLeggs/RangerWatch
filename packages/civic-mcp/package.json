{
  "name": "@rangerwatch/civic-mcp",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "scripts": {
    "start": "bun run src/index.ts",
    "lint": "eslint src",
    "typecheck": "tsc --noEmit",
    "test": "bun test",
    "build": "tsc --outDir dist",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@civic/hook-common": "^0.9.0",
    "@rangerwatch/shared": "workspace:*",
    "dotenv": "^17.2.3",
    "jose": "^6.2.2"
  },
  "devDependencies": {
    "@types/bun": "^1.3.11"
  }
}
